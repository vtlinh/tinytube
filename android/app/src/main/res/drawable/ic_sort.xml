<?xml version="1.0" encoding="utf-8"?>
<!-- Three lines, longest first: the conventional "this list is in an order"
     glyph. Which order is in the toolbar's subtitle, because the icon cannot
     say and a list silently re-ordering is worse than one that explains. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="@color/text">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M3,18h6v-2H3v2zM3,6v2h18V6H3zM3,13h12v-2H3v2z" />
</vector>
