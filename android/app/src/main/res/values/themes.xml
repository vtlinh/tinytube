<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- DayNight, with the colours resolved from values/ or values-night/. The
         app pins itself to night in App.kt; the parent is DayNight anyway so
         that unpinning it is a one-line change rather than a re-theme. -->
    <style name="Theme.TinyTube" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent</item>
        <item name="colorOnPrimary">@color/on_accent</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="android:colorBackground">@color/bg</item>
        <item name="android:textColorPrimary">@color/text</item>

        <!-- Without this the window starts on the platform default — white —
             and every cold launch flashes white before the grid draws, which
             is the single most obvious way a dark app looks unfinished. -->
        <item name="android:windowBackground">@color/bg</item>

        <item name="android:statusBarColor" tools:targetApi="21">@color/bg</item>
        <item name="android:navigationBarColor" tools:targetApi="21">@color/bg</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">@bool/light_status_bar</item>
    </style>

    <!-- The player is black in both palettes and always was: a video should
         have nothing around it, and anything showing at the margins reads as a
         rendering glitch. It does not follow the theme. -->
    <style name="Theme.TinyTube.Player" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:colorBackground">@color/black</item>
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor" tools:targetApi="21">@color/black</item>
        <item name="android:navigationBarColor" tools:targetApi="21">@color/black</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">false</item>
    </style>

    <!-- The toolbar sits on @color/surface, so its title and up arrow need to
         be tinted for that rather than inheriting the window's. -->
    <style name="ThemeOverlay.TinyTube.Toolbar" parent="ThemeOverlay.MaterialComponents.Toolbar.Surface">
        <item name="android:textColorPrimary">@color/text</item>
        <item name="colorControlNormal">@color/text</item>
    </style>

</resources>
