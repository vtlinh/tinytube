<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg">

    <!-- Hidden while the device's own lock is doing the work: the system
         prompt is the whole interface in that case, and an arithmetic
         question sitting behind it would look like a second challenge. It
         appears only on a device with no lock set up, or if the prompt
         itself fails. See ChallengeActivity. -->
    <LinearLayout
        android:id="@+id/fallback"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/challenge_title"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="@color/text" />

        <TextView
            android:id="@+id/question"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="20dp"
            android:textSize="20sp"
            android:lineSpacingMultiplier="1.4"
            android:textColor="@color/text" />

        <EditText
            android:id="@+id/answer_x"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:hint="@string/challenge_hint_x"
            android:inputType="number"
            android:imeOptions="actionNext"
            android:maxLength="6"
            android:textSize="20sp"
            android:textColor="@color/text"
            android:textColorHint="@color/text_muted"
            android:importantForAutofill="no"
            android:autofillHints="" />

        <EditText
            android:id="@+id/answer_y"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:hint="@string/challenge_hint_y"
            android:inputType="number"
            android:imeOptions="actionDone"
            android:maxLength="6"
            android:textSize="20sp"
            android:textColor="@color/text"
            android:textColorHint="@color/text_muted"
            android:importantForAutofill="no"
            android:autofillHints="" />

        <TextView
            android:id="@+id/error"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="10dp"
            android:textSize="15sp"
            android:textColor="@color/accent" />

        <Button
            android:id="@+id/submit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="@string/challenge_submit"
            android:backgroundTint="@color/accent"
            android:textColor="@color/on_accent" />

        <Button
            android:id="@+id/cancel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="@android:string/cancel"
            style="?android:attr/borderlessButtonStyle"
            android:textColor="@color/text_muted" />

    </LinearLayout>

</ScrollView>
